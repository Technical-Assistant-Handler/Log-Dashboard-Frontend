import 'package:flutter/material.dart';

class ColorTheme {
  // Text colors
  static const Color textColorPrimary = Color.fromARGB(255, 255, 255, 255);

  // Login button color
  static const Color loginButtonColor = Colors.blue;

  // Logout button color
  static const Color logoutButtonColor = Colors.red;

  // Box color
  static const Color boxColor = Colors.black54;

  static var buttonDisabledColor = const Color.fromARGB(255, 46, 75, 98);

  static var buttonColor; // Black with 67% opacity
}
